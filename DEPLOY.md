# 🚀 Инструкция по деплою

## ✅ Что уже сделано локально:

1. ✅ Установлена библиотека `github-readme-terminal`
2. ✅ Установлен FFmpeg
3. ✅ Создан `.env` с токеном (не коммитится)
4. ✅ Создан `generate_stats.py` (скрипт генерации)
5. ✅ Создан `.github/workflows/update-stats.yml` (автоматизация)
6. ✅ Обновлён `README.md` со ссылкой на GIF
7. ✅ Локально протестирована генерация GIF

## 📋 Что нужно сделать на GitHub:

### Шаг 1: Убедитесь, что репозиторий называется `Gr00ss`

Специальный репозиторий профиля должен иметь имя, совпадающее с вашим username.

### Шаг 2: Добавьте GitHub токен в Secrets

1. Перейдите в репозиторий: https://github.com/Gr00ss/Gr00ss
2. **Settings** → **Secrets and variables** → **Actions**
3. **New repository secret**
   - Name: `GH_TOKEN`
   - Secret: (ваш токен из `.env` файла)
4. **Add secret**

### Шаг 3: Включите GitHub Actions

1. **Settings** → **Actions** → **General**
2. **Workflow permissions** → ✅ **Read and write permissions**
3. **Save**

### Шаг 4: Загрузите файлы на GitHub

```bash
# Убедитесь, что вы в директории проекта
cd "C:\Users\ZOV\Documents\Gr00ss"

# Проверьте статус (убедитесь, что .env НЕ добавлен)
git status

# Добавьте файлы
git add .
git add output.gif

# Закоммитьте
git commit -m "🎉 Setup automated GitHub stats with Actions"

# Загрузите на GitHub
git push origin main
```

### Шаг 5: Запустите workflow вручную

1. Перейдите в репозиторий на GitHub
2. **Actions** → **Update GitHub Stats**
3. **Run workflow** → **Run workflow**
4. Дождитесь завершения (~1-2 минуты)

### Шаг 6: Проверьте результат

1. Обновите страницу профиля: https://github.com/Gr00ss
2. Должен появиться анимированный GIF с вашей статистикой!

---

## 🔧 Возможные проблемы

### ❌ Actions не запускается
**Решение:** Settings → Actions → General → Workflow permissions → ✅ Read and write permissions

### ❌ Ошибка "GH_TOKEN not found"
**Решение:** Проверьте, что секрет создан с точным именем `GH_TOKEN`

### ❌ GIF не отображается на профиле
**Решение:** 
1. Убедитесь, что `output.gif` есть в репозитории
2. Путь в README правильный: `https://raw.githubusercontent.com/Gr00ss/Gr00ss/main/output.gif`
3. Очистите кеш браузера (Ctrl+F5)

### ❌ Workflow падает с ZeroDivisionError
**Решение:** Это баг библиотеки. Скрипт уже обрабатывает эту ошибку и создаёт fallback версию GIF

---

## 📅 Автоматизация

После успешного деплоя:
- ✅ GIF будет обновляться автоматически каждый день в 00:00 UTC
- ✅ Можно запускать обновление вручную через Actions
- ✅ Изменения коммитятся автоматически ботом GitHub Actions

---

## 🎨 Настройка (опционально)

### Изменить расписание обновлений

Отредактируйте `.github/workflows/update-stats.yml`:

```yaml
# Каждые 6 часов
- cron: '0 */6 * * *'

# Каждую неделю
- cron: '0 0 * * 1'

# Каждый час
- cron: '0 * * * *'
```

### Изменить дизайн терминала

Отредактируйте `generate_stats.py`:
- Измените размеры: `Terminal(width=800, height=450, ...)`
- Измените цвета: используйте ANSI коды (`\x1b[31m` = красный)
- Добавьте свои данные: модифицируйте функцию `create_simple_gif()`

---

## 🎯 Готово!

После выполнения всех шагов ваш профиль будет автоматически обновляться с актуальной статистикой! 🚀
